module.exports = (tr, sourceEl, targetEl) => {
  if (sourceEl === targetEl) {
    return tr
  }

  // TODO Find common ancestor

  sourceEl.affine

  // TODO transform to root (layer) from source

  // TODO transform from root (layer) to target
}
