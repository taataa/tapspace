// Simulate attraction between points.
//
// Each point is { x, y, attractors }
// Where attractors is an array of N attractor objects.
// Where attractor object is { index, force }
// Where force is a real number. It describes a force
// towards to (or away from, if negative) the node at this index.
// Where seed is an integer for pseudorandomness generator.
//
// Points are initially pseudo-randomly positioned
// and then a simulation is ran where the points are reordered
// by attraction force.
//

module.exports = (points, seed) {
  throw new Error('Not implemented')
}
