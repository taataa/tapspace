module.exports = function (path) {
  // TODO
  // fitSize(path) translates and resizes `this` so that the rectangle encloses
  // the given `Path` precisely. The local size and the width-height ratio are
  // probably altered.
  //
  throw new Error('not implemented')
}
