module.exports = function (path) {
  // TODO
  // fitScale(path) translates and scales this so that the rectangle encloses
  // the given `Path` precisely. The local size is not altered,
  // only the scale and position.
  //
  throw new Error('not implemented')
}
