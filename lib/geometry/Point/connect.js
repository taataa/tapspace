module.exports = function (target) {
  // Connect points by moving space elements.
  //
  // Move the basis of this point relative to the basis of the given point.
  // After the move the points represent the same point in affine space.
  // The points are kind of matched. Modifies this.basis.tran.
  //
  // Parameters:
  //   target
  //     a Point, the target point
  //
  // Return
  //   this, for chaining .connect(...).connect(...).connect(...)
  //
  throw new Error('not implemented')
}
